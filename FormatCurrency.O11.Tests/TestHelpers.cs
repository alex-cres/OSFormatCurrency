namespace FormatCurrency.Tests;

public partial class TestHelpers
{
    // TODO: Add O11 adapter types (interface wrapper, struct wrapper)
    //       so test files can be byte-for-byte identical to the ODC versions.
}
